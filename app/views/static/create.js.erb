function refreshForm(innerHTML) {
  const newForm = document.getElementById("contact-form");
  newForm.innerHTML = innerHTML;
}
function renderFlash(innerHTML) {
  const flashField = document.getElementById("flash-message");
  flashField.innerHTML = innerHTML;
}
refreshForm("<%= j render 'contact_form' %>");
renderFlash("<%= j render 'shared/flash' %>");